server:
  port: 8070

spring:
 datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://127.0.0.1:3306/stockproject?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=UTF-8
   username: root
   password: root
 thymeleaf:
   prefix: classpath:/static/

mybatis:
  typeAliasesPackage: com.sunhao.stockprojecteg.model
  mapperLocations: classpath:mybatis.mapper/*.xml

logging:
  level:
    com.tedu.mapper: debug
